# ba-toolbox

Utility scripts for shell and developer workflows.

## bash/claude_wrapper_shell.sh

A shell wrapper for the Claude CLI that automatically injects a session name and terminal color on every invocation.

Run `claude -H` for full usage and install instructions.
